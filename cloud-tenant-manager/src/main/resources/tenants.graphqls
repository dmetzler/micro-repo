type Tenant {
  id: String
  customerId: String
  environmentType: String
  ready: Boolean
}

type Query {
  allTenantsByCustomer(customerId: String): [Tenant]
  tenantById(tenantId: String): Tenant
}

type Mutation {
  newTenant(customerId: String!, tenantId: String!): Tenant
}